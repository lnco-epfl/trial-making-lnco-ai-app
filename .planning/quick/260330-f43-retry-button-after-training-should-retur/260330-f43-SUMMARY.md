# Quick Task 260330-f43 - Summary

## What Changed
- Replaced keyboard-based retry message screens with explicit button-based retry screens in both `buildPractice1` and `buildPractice2`.
- Button label now reads "Back to instructions" (EN) / "Revenir aux instructions" (FR).
- Existing retry structure remains: retry message -> instruction screen (with preview) -> second attempt.

## Files Updated
- `src/modules/experiment/parts/practice.ts`
- `src/langs/en.json`
- `src/langs/fr.json`

## Outcome
Participants who fail practice now explicitly choose to return to the beginning of instructions before retrying, matching desired UX.
