---
phase: 01-patient-instructions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/langs/en.json
  - src/langs/fr.json
autonomous: true
requirements:
  - INSTR-01
  - INSTR-02
  - INSTR-03
  - INSTR-04
  - INSTR-05

must_haves:
  truths:
    - "All TRAIL_MAKING keys in en.json contain the neuropsychologist-authored English text verbatim (no placeholder or generic strings remain)"
    - "All TRAIL_MAKING keys in fr.json contain the neuropsychologist-authored French text verbatim"
    - "Both files include new keys for conditional retry messages, review button text, Part A and Part B intro texts, task ready texts, and completion texts"
    - "Language switch (?lang=en / ?lang=fr) will surface the correct language text through i18next because keys are present in both files"
    - "Numbers-only (Part A) and numbers+letters (Part B) variants have clearly differentiated instruction text in both languages"
  artifacts:
    - path: "src/langs/en.json"
      provides: "English i18n strings for all TRAIL_MAKING instruction screens"
      contains: "TRAIL_MAKING.PRACTICE1_INTRO"
    - path: "src/langs/fr.json"
      provides: "French i18n strings for all TRAIL_MAKING instruction screens"
      contains: "TRAIL_MAKING.PRACTICE1_INTRO"
  key_links:
    - from: "src/modules/experiment/parts/practice.ts"
      to: "src/langs/en.json"
      via: "i18n.t('TRAIL_MAKING.PRACTICE1_INTRO') and related keys"
      pattern: "TRAIL_MAKING\\.PRACTICE1_INTRO"
    - from: "src/modules/experiment/parts/task-core.ts"
      to: "src/langs/en.json"
      via: "i18n.t('TRAIL_MAKING.TASK1_READY_MESSAGE') etc."
      pattern: "TRAIL_MAKING\\.TASK1_READY_MESSAGE"
---

<objective>
Replace all placeholder TRAIL_MAKING i18n strings in en.json and fr.json with the neuropsychologist-authored text from the CONTEXT.md, and add all new keys required by the conditional retry and review-button features.

Purpose: Without correct instruction text, PD patients cannot self-administer the task. Every screen must show clinically-accurate content in both languages.
Output: en.json and fr.json with complete, verbatim neuropsychologist-authored TRAIL_MAKING content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-patient-instructions/1-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update TRAIL_MAKING keys in en.json with neuropsychologist-authored English text</name>
  <files>src/langs/en.json</files>
  <read_first>
    - src/langs/en.json (current state — must see all existing TRAIL_MAKING keys before editing)
    - .planning/phases/01-patient-instructions/1-CONTEXT.md (verbatim text source — the &lt;specifics&gt; section)
  </read_first>
  <action>
Replace and add keys in the `translations.TRAIL_MAKING` object in `src/langs/en.json`.

**Keys to UPDATE (replace generic placeholder values with exact text below):**

`WELCOME_TITLE`: `"Trail Making Test (TMT)"`
`WELCOME_MESSAGE`: `"Welcome. This test helps us understand how your brain works. Please read each instruction carefully before you begin."`
(per D-03: warm, accessible for PD patients — Claude's discretion)
`START_BUTTON`: keep as-is `"Start Fullscreen"` (per D-15)

**Remove these old keys** (they are replaced by the new structured keys below):
- `INSTRUCTIONS_TITLE`, `INSTRUCTIONS_OVERVIEW`, `INSTRUCTIONS_GOAL`, `INSTRUCTIONS_HOW_TO_TITLE`, `INSTRUCTIONS_CLICK`, `INSTRUCTIONS_SEQUENCE`, `INSTRUCTIONS_ERROR`, `PRACTICE_INTRO_TITLE`, `PRACTICE_INTRO_MESSAGE`, `READY_MESSAGE`, `START_PRACTICE_BUTTON`, `PRACTICE1_TITLE`, `PRACTICE1_MESSAGE`, `PRACTICE2_TITLE`, `PRACTICE2_MESSAGE`, `PRACTICE_COMPLETE_TITLE`, `TASK1_READY_TITLE`, `TASK2_READY_TITLE`

**Add these new keys** with the EXACT text below (verbatim from neuropsychologist document):

`PRACTICE1_INTRO`: `"We will start with the instructions and a short training. Read and listen carefully.\nYou can see here circles with numbers from 1 to 8. Your goal is to connect all numbers by clicking on the numbers in the correct numerical order to connect them. The starting point is marked at 1 (Start) and the ending point at 8 (End). Connect the numbers from 1 to 2, from 2 to 3, and so on until number 8 (End). A line will be drawn between circles as you connect them.\nImportant: If you make a mistake, click the last circle again to reset it, then continue with the correct sequence.\nWork as quickly and accurately as possible. Ready? Go ahead and try. This is a training before we start the actual test."`

`PRACTICE1_REVIEW_BUTTON`: `"If needed, click here to review the instructions and do the training once more."`

`PRACTICE1_RETRY_MESSAGE`: `"OK! Don't worry, we'll repeat the training once more."`

`PRACTICE1_PROCEED_MESSAGE`: `"OK! Now you'll perform the actual test."`

`TASK1_READY_MESSAGE`: `"You now see numbers from 1 to 25. The starting point is marked at 1 (Start) and the ending point at 25 (End). Just like during the training, you must connect the numbers to each other in ascending order until number 25 (End). Remember: Work as quickly and accurately as possible. The time for completing the test will be measured.\nImportant: If you make a mistake, click on the last circle again to reset it, then continue with the correct sequence.\nAre you ready? Go ahead."`

`TASK1_COMPLETE_MESSAGE`: `"Thank you, this part of the test is finished. We will now move on to the second part."`

`PRACTICE2_INTRO`: `"This part is slightly different. We will start with a short training with the instructions. Read and listen carefully.\nYou can see here again circles with numbers but also with letters. Your goal is to connect one number with one letter by following the numerical and alphabetical order. The starting point is marked at 1 (Start) and the ending point at D (End). Begin by clicking on 1 (Start), then A, then from A to 2, from 2 to B, and so on until the end. A line will be drawn between circles as you connect them. Remember to alternate one number with one letter in increasing and alphabetical order.\nImportant: If you make a mistake, click the last circle again to reset it, then continue with the correct sequence.\nWork as quickly and accurately as possible. Ready? Go ahead and try. It's a training."`

`PRACTICE2_REVIEW_BUTTON`: `"If needed, click here to review the instructions and do the training once more."`

`PRACTICE2_RETRY_MESSAGE`: `"OK! Don't worry, we'll repeat the training once more."`

`PRACTICE2_PROCEED_MESSAGE`: `"OK! Now you'll perform the actual test."`

`TASK2_READY_MESSAGE`: `"You now see the numbers and letters scattered. The starting point is marked at 1 (Start) and the ending point at 13 (End). Just like during the training, you must connect the numbers and letters the same way, that is, first a number, then a letter until the end. Remember to alternate one number with one letter in increasing and alphabetical order. Click on 1 (Start), then A, from A to 2, from 2 to B, and so on until the end. A line will be drawn between circles as you connect them.\nImportant: If you make a mistake, click the last circle again to reset it, then continue with the correct sequence.\nWork as quickly and accurately as possible. The time taken to complete the test will be measured.\nReady? Go ahead."`

`TASK2_COMPLETE_MESSAGE`: `"OK, thank you, the test is now complete."`

`START_LABEL`: `"Start"`

`END_LABEL`: `"End"`

**Keep unchanged** (per D-14): `CONTINUE_BUTTON`, `PRESS_TO_BEGIN`, `PRESS_TO_CONTINUE`, `DONE_BUTTON`, `RETRY_BUTTON`, `CONTINUE_BUTTON_INLINE`, `PRACTICE_TRIAL_SUCCESS`, `PRACTICE_TRIAL_ERROR`, `PRACTICE_TRIAL_RETRY_PROMPT`, `TIME_TAKEN`, `SECONDS`, `ERRORS_SELF_CORRECTED`, `ERRORS_NON_SELF_CORRECTED`

Keep TASK1_COMPLETE_TITLE and TASK2_COMPLETE_TITLE if they are used for the HTML heading of the completion screen, but use the new TASK1_COMPLETE_MESSAGE/TASK2_COMPLETE_MESSAGE values for the body text.
  </action>
  <verify>
    <automated>node -e "const f = JSON.parse(require('fs').readFileSync('src/langs/en.json','utf8')); const tm = f.translations.TRAIL_MAKING; const required = ['PRACTICE1_INTRO','PRACTICE1_REVIEW_BUTTON','PRACTICE1_RETRY_MESSAGE','PRACTICE1_PROCEED_MESSAGE','TASK1_READY_MESSAGE','TASK1_COMPLETE_MESSAGE','PRACTICE2_INTRO','PRACTICE2_REVIEW_BUTTON','PRACTICE2_RETRY_MESSAGE','PRACTICE2_PROCEED_MESSAGE','TASK2_READY_MESSAGE','TASK2_COMPLETE_MESSAGE','START_LABEL','END_LABEL']; const missing = required.filter(k => !tm[k]); if(missing.length) { console.error('MISSING KEYS:', missing); process.exit(1); } console.log('All required keys present'); required.forEach(k => console.log(k, ':', tm[k].substring(0,60)));"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PRACTICE1_INTRO" src/langs/en.json` returns 1
    - `grep -c "PRACTICE2_INTRO" src/langs/en.json` returns 1
    - `grep -c "START_LABEL" src/langs/en.json` returns 1
    - `grep -c "END_LABEL" src/langs/en.json` returns 1
    - `grep "TASK1_COMPLETE_MESSAGE" src/langs/en.json` output contains "this part of the test is finished"
    - `grep "TASK2_COMPLETE_MESSAGE" src/langs/en.json` output contains "test is now complete"
    - `grep "PRACTICE1_RETRY_MESSAGE" src/langs/en.json` output contains "Don't worry"
    - `grep "PRACTICE1_PROCEED_MESSAGE" src/langs/en.json` output contains "Now you'll perform the actual test"
    - `grep "TASK1_READY_MESSAGE" src/langs/en.json` output contains "numbers from 1 to 25"
    - `grep "TASK2_READY_MESSAGE" src/langs/en.json` output contains "numbers and letters scattered"
    - `node -e "JSON.parse(require('fs').readFileSync('src/langs/en.json','utf8'))"` exits 0 (valid JSON)
  </acceptance_criteria>
  <done>en.json TRAIL_MAKING section contains all new keys with exact neuropsychologist-authored text; JSON is valid; no placeholder generic strings remain for instruction screens.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update TRAIL_MAKING keys in fr.json with neuropsychologist-authored French text</name>
  <files>src/langs/fr.json</files>
  <read_first>
    - src/langs/fr.json (current state — must see all existing TRAIL_MAKING keys before editing)
    - .planning/phases/01-patient-instructions/1-CONTEXT.md (verbatim French text source — the &lt;specifics&gt; FRENCH section)
  </read_first>
  <action>
Replace and add keys in the `translations.TRAIL_MAKING` object in `src/langs/fr.json`. Apply the same structural changes as Task 1 (same keys removed, same keys added) but with the French text below.

**Keys to UPDATE:**
`WELCOME_TITLE`: `"Trail Making Test (TMT)"`
`WELCOME_MESSAGE`: `"Bienvenue. Ce test nous aide à comprendre le fonctionnement de votre cerveau. Veuillez lire attentivement chaque instruction avant de commencer."`
`START_BUTTON`: keep as-is `"Démarrer le plein écran"` (per D-15)

**Remove the same old keys as in Task 1** (same list: `INSTRUCTIONS_TITLE`, `INSTRUCTIONS_OVERVIEW`, etc.)

**Add these new keys** with EXACT French text (verbatim from neuropsychologist document):

`PRACTICE1_INTRO`: `"Nous allons commencer par les instructions et par un exemple pour vous entraîner à faire ce test. Lisez et écoutez attentivement ces instructions.\nVous verrez des cercles dans lesquels sont inscrits les chiffres de 1 à 8. Votre objectif est de cliquer sur les chiffres pour les relier dans le bon ordre, c'est-à-dire l'ordre croissant. Le début est indiqué au chiffre 1 (Début) et la fin au chiffre 8 (Fin). Vous allez donc relier de 1 à 2, de 2 à 3, et ainsi de suite jusqu'au numéro 8. Une ligne sera tracée entre les cercles au fur et à mesure que vous les reliez.\nTravaillez aussi rapidement et précisément que possible. Êtes-vous prêt(e) ? Allez-y ! C'est l'entraînement."`

`PRACTICE1_REVIEW_BUTTON`: `"Si vous en avez besoin, cliquez ici pour revoir les instructions et faire l'entraînement une fois de plus."`

`PRACTICE1_RETRY_MESSAGE`: `"OK ! Ne vous inquiétez pas, vous allez répéter l'entraînement."`

`PRACTICE1_PROCEED_MESSAGE`: `"Très bien ! Vous allez maintenant commencer le test."`

`TASK1_READY_MESSAGE`: `"Vous voyez maintenant des numéros de 1 à 25. Vous devez commencer au 1 (Début) et terminer au 25 (Fin). Comme pendant l'entraînement, vous devez relier tous les numéros entre eux dans l'ordre croissant jusqu'au numéro 25 (Fin).\nImportant : Si vous faites une erreur, cliquez à nouveau sur le dernier cercle pour le décocher ou l'annuler, puis continuez avec la séquence correcte.\nTravaillez aussi rapidement et précisément que possible. Le temps que vous mettrez à compléter le test sera enregistré. Êtes-vous prêt(e) ? Allez-y !"`

`TASK1_COMPLETE_MESSAGE`: `"Merci, cette partie du test est faite, nous allons passer à la 2ème partie."`

`PRACTICE2_INTRO`: `"Cette partie est légèrement différente. Nous allons commencer par un court entraînement avec les instructions. Lisez et écoutez attentivement.\nVous voyez ici à nouveau des cercles avec des numéros mais aussi des lettres. Votre objectif est de relier un numéro à une lettre en suivant l'ordre numérique et alphabétique. Le début est indiqué au 1 (Début) et la fin au D (Fin). Commencez par cliquer sur 1 (Début), puis sur A, ensuite de A à 2, de 2 à B, de B à 3 et ainsi de suite jusqu'à la fin. Une ligne sera tracée entre les cercles au fur et à mesure que vous les reliez. N'oubliez pas, les cercles sont donc connectés en alternance, un chiffre-une lettre, un chiffre-une lettre dans l'ordre croissant et alphabétique.\nTravaillez aussi rapidement et précisément que possible. Êtes-vous prêt(e) ? Allez-y ! C'est l'entraînement."`

`PRACTICE2_REVIEW_BUTTON`: `"Si vous en avez besoin, cliquez ici pour revoir les instructions et faire l'entraînement une fois de plus."`

`PRACTICE2_RETRY_MESSAGE`: `"OK ! Ne vous inquiétez pas, vous allez répéter l'entraînement."`

`PRACTICE2_PROCEED_MESSAGE`: `"Très bien ! Vous allez maintenant commencer le test."`

`TASK2_READY_MESSAGE`: `"Vous voyez maintenant les chiffres et les lettres éparpillés. Le début est indiqué au 1 (Début) et la fin au 13 (Fin). N'oubliez pas, les cercles sont donc connectés en alternance, un chiffre-une lettre, un chiffre-une lettre dans l'ordre croissant et alphabétique. Commencez par cliquer sur 1 (Début), puis sur A, ensuite de A à 2, puis de 2 à B, de B à 3 et continuez comme ça jusqu'au 13 (Fin). Une ligne se dessinera automatiquement entre les cercles au fur et à mesure que vous les reliez.\nImportant : Si vous faites une erreur, cliquez à nouveau sur le dernier cercle pour le décocher ou l'annuler, puis continuez avec la séquence correcte.\nTravaillez aussi rapidement et précisément que possible. Le temps que vous mettrez à compléter le test sera enregistré. Êtes-vous prêt(e) ? Allez-y !"`

`TASK2_COMPLETE_MESSAGE`: `"OK, merci, le test est maintenant terminé."`

`START_LABEL`: `"Début"`

`END_LABEL`: `"Fin"`

**Keep unchanged** (per D-14): same functional keys as in Task 1 (DONE_BUTTON, RETRY_BUTTON, etc.)
  </action>
  <verify>
    <automated>node -e "const f = JSON.parse(require('fs').readFileSync('src/langs/fr.json','utf8')); const tm = f.translations.TRAIL_MAKING; const required = ['PRACTICE1_INTRO','PRACTICE1_REVIEW_BUTTON','PRACTICE1_RETRY_MESSAGE','PRACTICE1_PROCEED_MESSAGE','TASK1_READY_MESSAGE','TASK1_COMPLETE_MESSAGE','PRACTICE2_INTRO','PRACTICE2_REVIEW_BUTTON','PRACTICE2_RETRY_MESSAGE','PRACTICE2_PROCEED_MESSAGE','TASK2_READY_MESSAGE','TASK2_COMPLETE_MESSAGE','START_LABEL','END_LABEL']; const missing = required.filter(k => !tm[k]); if(missing.length) { console.error('MISSING KEYS:', missing); process.exit(1); } console.log('All required keys present'); required.forEach(k => console.log(k, ':', tm[k].substring(0,60)));"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PRACTICE1_INTRO" src/langs/fr.json` returns 1
    - `grep -c "PRACTICE2_INTRO" src/langs/fr.json` returns 1
    - `grep -c "START_LABEL" src/langs/fr.json` returns 1
    - `grep -c "END_LABEL" src/langs/fr.json` returns 1
    - `grep "TASK1_COMPLETE_MESSAGE" src/langs/fr.json` output contains "passer à la 2ème partie"
    - `grep "TASK2_COMPLETE_MESSAGE" src/langs/fr.json` output contains "test est maintenant terminé"
    - `grep "PRACTICE1_RETRY_MESSAGE" src/langs/fr.json` output contains "Ne vous inquiétez pas"
    - `grep "PRACTICE1_PROCEED_MESSAGE" src/langs/fr.json` output contains "commencer le test"
    - `grep "START_LABEL" src/langs/fr.json` output contains "Début"
    - `grep "END_LABEL" src/langs/fr.json` output contains "Fin"
    - `node -e "JSON.parse(require('fs').readFileSync('src/langs/fr.json','utf8'))"` exits 0 (valid JSON)
  </acceptance_criteria>
  <done>fr.json TRAIL_MAKING section contains all new keys with exact neuropsychologist-authored French text; JSON is valid; no placeholder generic strings remain for instruction screens.</done>
</task>

</tasks>

<verification>
- `node -e "JSON.parse(require('fs').readFileSync('src/langs/en.json','utf8'))"` — exits 0
- `node -e "JSON.parse(require('fs').readFileSync('src/langs/fr.json','utf8'))"` — exits 0
- `grep "Don.t worry" src/langs/en.json` — matches PRACTICE1_RETRY_MESSAGE
- `grep "Ne vous inquiétez" src/langs/fr.json` — matches PRACTICE1_RETRY_MESSAGE
- `grep "this part of the test is finished" src/langs/en.json` — matches TASK1_COMPLETE_MESSAGE
- `grep "passer à la 2ème partie" src/langs/fr.json` — matches TASK1_COMPLETE_MESSAGE
- Both files remain valid JSON (no syntax errors introduced)
</verification>

<success_criteria>
- All 14 new/updated TRAIL_MAKING keys are present in both en.json and fr.json
- Text is verbatim from the neuropsychologist-authored document (per D-01, D-02)
- en.json TASK1_COMPLETE_MESSAGE = "Thank you, this part of the test is finished. We will now move on to the second part."
- fr.json TASK1_COMPLETE_MESSAGE = "Merci, cette partie du test est faite, nous allons passer à la 2ème partie."
- Both files are syntactically valid JSON
- Functional keys (DONE_BUTTON, RETRY_BUTTON, etc.) are unchanged (per D-14)
</success_criteria>

<output>
After completion, create `.planning/phases/01-patient-instructions/01-01-SUMMARY.md`
</output>
